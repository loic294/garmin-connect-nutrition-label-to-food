# Anthropic API key (required)
ANTHROPIC_API_KEY=sk-ant-...

# Port to expose on the NAS (default: 8080)
PORT=8080

# Comma-separated list of allowed CORS origins.
# Use * to allow all (fine for LAN-only use).
# Example: http://192.168.1.100:8080,https://nutrition.nas.local
ALLOWED_ORIGINS=*
